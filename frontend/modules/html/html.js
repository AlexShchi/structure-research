import './html.scss'

function html() {
    // document.addEventListener('click', function(e){
    //     if ( e.target.className == 'js-next-level' ) {
    //         e.preventDefault();
    //         e.target.closest('.schema_row').nextElementSibling.classList.add('is_active');
    //     }
    // })

}

export{html};
